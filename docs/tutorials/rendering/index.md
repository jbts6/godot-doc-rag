allow_comments
False

# Rendering

Most rendering topics are covered in `2D rendering <doc_2d_rendering>` and `3D rendering <doc_3d_rendering>`.

renderers viewports multiple_resolutions jitter_stutter compositor hdr_output